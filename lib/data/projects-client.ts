import type { GameGenre, GameProject } from '@/lib/data/game-lab-types'
export async function createProject(input: { name: string; description?: string; genre: GameGenre }) { const res = await fetch('/api/projects', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(input) }); if (!res.ok) throw new Error(); return await res.json() as GameProject }
export async function updateProject(id: string, input: Partial<Pick<GameProject, 'name' | 'description' | 'genre' | 'status' | 'cover_image_url' | 'metadata'>>) { const res = await fetch(`/api/projects/${id}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(input) }); if (!res.ok) throw new Error(); return await res.json() as GameProject }
export async function deleteProject(id: string) { const res = await fetch(`/api/projects/${id}`, { method: 'DELETE' }); if (!res.ok) throw new Error() }
