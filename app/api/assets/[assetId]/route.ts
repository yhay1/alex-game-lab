import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { ASSET_BUCKET } from '@/lib/data/assets'
export async function DELETE(_: Request, { params }: { params: Promise<{ assetId: string }> }) { const { assetId } = await params; const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); const { data: asset, error } = await supabase.from('game_assets').select('*').eq('id', assetId).eq('owner_id', user.id).single(); if (error || !asset) return NextResponse.json({ error: 'Asset not found' }, { status: 404 }); await supabase.storage.from(ASSET_BUCKET).remove([asset.storage_path]); const deleted = await supabase.from('game_assets').delete().eq('id', assetId).eq('owner_id', user.id); if (deleted.error) return NextResponse.json({ error: deleted.error.message }, { status: 500 }); return new NextResponse(null, { status: 204 }) }
