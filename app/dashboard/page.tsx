import Link from 'next/link'
import { redirect } from 'next/navigation'
import { ArrowRight, FolderKanban, Plus, Sparkles } from 'lucide-react'
import { getCurrentUser, getProfile, listProjects } from '@/lib/data/game-lab'
import { GameLabShell, WorkspacePlaceholder } from '@/components/game-lab-shell'

export default async function DashboardPage() { const user = await getCurrentUser(); if (!user) redirect('/auth/sign-in'); const [profile, projects] = await Promise.all([getProfile(user.id).catch(() => null), listProjects()]); const name = profile?.display_name ?? user.email?.split('@')[0] ?? 'Creator'; return <GameLabShell name={name}><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="font-mono text-xs uppercase tracking-[0.2em] text-primary">Creator dashboard</p><h1 className="mt-2 text-3xl font-semibold tracking-tight md:text-4xl">Good to see you, {name}.</h1><p className="mt-2 text-muted-foreground">Everything you need to shape your next 2D game.</p></div><Link href="/dashboard/create" className="inline-flex items-center gap-2 rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground"><Plus className="size-4" />Create Game</Link></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-border bg-card p-5"><FolderKanban className="size-5 text-primary" /><p className="mt-5 text-3xl font-semibold">{projects.length}</p><p className="mt-1 text-sm text-muted-foreground">Active projects</p></div><div className="rounded-2xl border border-border bg-card p-5"><Sparkles className="size-5 text-primary" /><p className="mt-5 text-3xl font-semibold">2D</p><p className="mt-1 text-sm text-muted-foreground">Game Lab format</p></div><div className="rounded-2xl border border-border bg-card p-5"><p className="text-sm font-semibold">Ready when you are</p><p className="mt-2 text-sm leading-6 text-muted-foreground">Create a project and keep every part of your game in one workspace.</p></div></div><div className="mt-10 flex items-center justify-between"><div><h2 className="text-xl font-semibold">Recent projects</h2><p className="mt-1 text-sm text-muted-foreground">Your latest work, pulled from Game Lab.</p></div><Link href="/dashboard/projects" className="inline-flex items-center gap-2 text-sm font-medium text-primary">View all <ArrowRight className="size-4" /></Link></div>{projects.length ? <div className="mt-4 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{projects.slice(0, 6).map(project => <Link key={project.id} href={`/dashboard/projects/${project.id}`} className="rounded-2xl border border-border bg-card p-5 transition-colors hover:border-primary/60"><div className="flex justify-between gap-3"><span className="rounded-full bg-secondary px-3 py-1 text-xs">{project.genre ?? 'Genre pending'}</span><span className="text-xs text-muted-foreground">{project.status}</span></div><h3 className="mt-7 font-semibold">{project.name}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-muted-foreground">{project.description ?? 'No description yet.'}</p></Link>)}</div> : <div className="mt-4 rounded-2xl border border-dashed border-border p-8"><p className="font-medium">No projects yet.</p><p className="mt-1 text-sm text-muted-foreground">Create your first game to start building.</p></div>}<div className="mt-10 grid gap-4 md:grid-cols-2"><WorkspacePlaceholder icon="sparkles" title="AI assistant" description="A future creative copilot will live alongside your project work." /><WorkspacePlaceholder icon="box" title="Your workspace" description="Levels, entities, properties, assets, and settings are organized per project." /></div></GameLabShell> }
