import Link from 'next/link'
import { redirect } from 'next/navigation'
import { ArrowLeft, Sparkles } from 'lucide-react'
import { getCurrentUser, getProfile } from '@/lib/data/game-lab'
import { ProjectForm } from '@/components/projects/project-form'
import { GameLabShell } from '@/components/game-lab-shell'

export default async function CreateGamePage() { const user = await getCurrentUser(); if (!user) redirect('/auth/sign-in'); const profile = await getProfile(user.id).catch(() => null); const name = profile?.display_name ?? user.email?.split('@')[0] ?? 'Creator'; return <GameLabShell name={name}><div className="mx-auto max-w-4xl"><Link href="/dashboard" className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4" />Home</Link><header className="mt-8"><p className="font-mono text-xs uppercase tracking-[0.2em] text-primary">Create / New project</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">Start with the foundation.</h1><p className="mt-3 max-w-2xl text-muted-foreground">Give your game a name, genre, and short brief. This creates a private project workspace you can continue shaping.</p></header><div className="mt-8 grid gap-6 lg:grid-cols-[1fr_280px]"><section className="rounded-3xl border border-border bg-card p-6 md:p-8"><ProjectForm /></section><aside className="rounded-3xl border border-primary/30 bg-primary/5 p-6"><Sparkles className="size-5 text-primary" /><h2 className="mt-4 font-semibold">AI assistance is coming soon</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">The current V0.1 flow stores a clear project brief without pretending to generate game content.</p></aside></div></div></GameLabShell> }
